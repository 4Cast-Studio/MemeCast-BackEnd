export type WalletCountAndVolume = {
  readonly wallet_count: number;
  readonly total_volume: number;
};

export type InteractionCount = {
  readonly interaction_count: number;
};

export type GameRounds = {
  readonly game_rounds: number;
};

export type RevenueShare = {
  readonly revenue_sharing: number;
};
